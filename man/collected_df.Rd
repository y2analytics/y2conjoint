% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collected-df.R
\name{collected_df}
\alias{collected_df}
\title{A data frame carrying collections of columns}
\usage{
collected_df(data, collections)
}
\arguments{
\item{data}{A data frame. It is coerced to a tibble internally.}

\item{collections}{The \link{collection}s that group the columns of \code{data}. Accepts
a single \link{collection}, a vector of them (via \code{\link[=c]{c()}}), or a list. Every level
of every collection must be a column of \code{data}.}
}
\value{
A \code{collected_df}.
}
\description{
A \code{collected_df} is a tibble subclass that carries a list of \link{collection}s as
metadata: each collection names a group of columns that belong together. It is
the base type behind \link{conjoint_df} (which adds an outside-good column and
column protection) and is also what \code{\link[=run_scenario]{run_scenario()}} returns, where each
collection groups the \verb{share_*} columns produced by one \link{competitive_set}.
}
\details{
Unlike a \code{conjoint_df}, a plain \code{collected_df} does not protect its columns:
ordinary dplyr and base operations may freely edit them. The class is
preserved through dplyr verbs where possible; if an operation removes some of
a collection's columns, that collection is dropped (with a warning) rather
than left dangling.
}
\examples{
shares <- tibble::tibble(share_A = 0.4, share_B = 0.6)

# A single collection need not be wrapped in a list.
collected_df(shares, collection("Launch", c("share_A", "share_B")))

# Several collections can be passed as a vector or a list.
two <- tibble::tibble(share_A = 0.4, share_B = 0.6)
collected_df(two, c(collection("A", "share_A"), collection("B", "share_B")))
}
