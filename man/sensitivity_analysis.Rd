% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{sensitivity_analysis}
\alias{sensitivity_analysis}
\title{Sensitivity analysis for a product specification}
\usage{
sensitivity_analysis(
  x,
  multiple_select,
  product,
  combine_fn = pmax,
  scaling_factor = 1
)
}
\arguments{
\item{x}{A \link{conjoint_df} of individual-level utilities.}

\item{multiple_select}{A character vector of collection names that may hold
more than one level at once. Collections not listed here are treated as
single-select.}

\item{product}{The \link{product} to analyse.}

\item{combine_fn}{How to combine multiple selected levels within a collection.
Passed through to \code{\link[=run_scenario]{run_scenario()}}. Either a single function applied to
every collection (the default, \code{\link[=pmax]{pmax()}}), or a named list mapping collection
names to functions.}

\item{scaling_factor}{A numeric multiplier applied to all utilities before the
softmax. Passed through to \code{\link[=run_scenario]{run_scenario()}}. Defaults to \code{1}.}
}
\value{
A tibble with one row per perturbation and the columns:
\code{Feature} (collection name), \code{Level} (the level added, removed, or
substituted), \code{comparison} (\code{"added_in"}, \code{"taken_out"}, or \code{"alone"}),
\code{preference_share} (share of the perturbed product), \code{baseline} (share of
the unchanged product), \code{delta} (\code{preference_share - baseline}), and
\code{product_name}.
}
\description{
Measures how a product's preference share responds to changing one level at a
time. The unchanged \link{product} is scored on its own (against the NONE outside
good) to give a \code{baseline} share, then each collection is perturbed
individually - never in combination with another collection - and re-scored:
}
\details{
\itemize{
\item \strong{Single-select} collections (the default) are perturbed by replacing the
current level with each other level in turn (\code{comparison = "alone"}).
\item \strong{Multiple-select} collections (those named in \code{multiple_select}) are
perturbed by adding each not-yet-selected level (\code{comparison = "added_in"})
and, when two or more levels are already selected, by removing each selected
level (\code{comparison = "taken_out"}).
}

An absence level (see \link{collection}) can never be paired with another level, so
for a multiple-select collection any addition that would pair with the absence
level is instead recorded as an \code{"alone"} swap (the candidate level on its
own) rather than an \code{"added_in"}.
}
\examples{
cjt <- conjoint_df(example_utilities, example_crosswalk)
flagship <- product(
  cjt,
  c("Northwind", "$299", "20 hours", "256 GB", "Black"),
  name = "Flagship"
)
sensitivity_analysis(cjt, multiple_select = "Brand", product = flagship)
}
