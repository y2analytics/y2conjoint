% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conjoint-df.R
\name{conjoint_df}
\alias{conjoint_df}
\title{Create a conjoint data frame}
\usage{
conjoint_df(data, crosswalk, none_col = "NONE")
}
\arguments{
\item{data}{A data frame of individual-level utilities. Level columns are
named in the model format \verb{A[NUM]B[NUM]}; extra columns are allowed. It is
coerced to a tibble internally.}

\item{crosswalk}{A data frame mapping levels to collections, with columns
\code{old_name} (the model-format column), \code{user_name} (the renamed column),
\code{collection_name}, and \code{collection_order} (an integer rank, or \code{NA} for
unordered collections). An optional logical \code{absence} column flags the
"absence" level of a collection (see \link{collection}); at most one level per
collection may be flagged, and it defaults to no absence levels when the
column is missing. It is coerced to a tibble internally, which gives
stricter column access (no partial matching) than a base data frame.}

\item{none_col}{The name of the outside-good column. Defaults to \code{"NONE"}.}
}
\value{
A \code{conjoint_df}.
}
\description{
\code{conjoint_df()} builds a tibble subclass that carries its conjoint structure
as metadata. Utility columns arrive in the model format (\verb{A[NUM]B[NUM]}) and
are renamed to their user-facing names; they are grouped into \link{collection}s
via the supplied \code{crosswalk}. Any columns not described by the crosswalk
(e.g. demographics or IDs) are kept as-is.
}
\details{
The level columns and the \code{none} column are \emph{protected}: dplyr and base
operations may freely edit other columns, but attempts to drop, rename, or
overwrite a protected column error. Convert with \code{\link[tibble:as_tibble]{tibble::as_tibble()}} first
to escape those guards.
}
\examples{
conjoint_df(example_utilities, example_crosswalk)
}
